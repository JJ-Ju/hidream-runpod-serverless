# Repository Instructions

- Use the `github.com-agent` SSH host for GitHub push, pull, and fetch operations instead of the normal `github.com` host.
- Preferred remote format: `git@github.com-agent:JJ-Ju/hidream-runpod-serverless.git`.
